const userInitialValue = {}

const userReducer = (state = userInitialValue, action) => {
    switch(action.type){
        default : {
            return state
        }
    }
}

export default userReducer